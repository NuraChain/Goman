node_modules/
dist/
dist-server/
.azeroth/
logs/
coverage/
*.tsbuildinfo
.env
*.local
.DS_Store
Thumbs.db
.idea
.vscode
.claude/settings.local.json
# The sqlite chain index (derived; the indexer replays it from the chain).
server/.data/
.data

# Uploaded market/category art (content-addressed; not source).
server/uploads/

# The Solidity workspace lives in its own repo now; this is the handoff archive of the
# last in-tree state. Local artefact, not source.
contract.zip
